// Kept as a compatibility no-op for callers while cloud login is handled at
// `/login` with email/password rather than a workspace-hostname selector.
export const useRedirectToCloudSelect = () => {};
